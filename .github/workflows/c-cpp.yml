name: C/C++ CI

on:
  push:
    paths-ignore:
      - 'README.md'
      - 'PROTOCOL.md'
      - 'tools/'
  pull_request:
    paths-ignore:
      - 'README.md'
      - 'PROTOCOL.md'
      - 'tools/'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      run: make test
